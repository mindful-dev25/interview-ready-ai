class ReportGenerator:
    """Placeholder final report generator."""

    async def generate(self, session_id: str) -> str:
        # TODO: Compile reviewed answers, guardrail notes, and evidence into a final report.
        return f"TODO: final report for session {session_id}"
